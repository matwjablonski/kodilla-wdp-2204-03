export const getAllDeals = state => state.deals;

const dealsReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default dealsReducer;
